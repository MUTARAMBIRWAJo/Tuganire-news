"use client"

import Image from "next/image"
import Link from "next/link"
import { useEffect, useMemo, useState } from "react"
import { ArrowRight, Bookmark, BookmarkCheck, Check, Clock3, Copy, Eye, Facebook, Search, Send, ShieldCheck, Sparkles, X } from "lucide-react"
import { t, type Locale } from "@/lib/i18n"
import { cleanArticlePreview } from "@/lib/content"
import { formatKigaliDate, type HomepageArticle } from "@/lib/homepage-data"

interface HomepageEditorialProps {
  articles: HomepageArticle[]
  hero: HomepageArticle | null
  mostRead: HomepageArticle[]
  locale: Locale
}

const BOOKMARKS_KEY = "tuganire-bookmarks"
const SEARCH_RECENTS_KEY = "tuganire-search-recents"

function StoryImage({ article, priority = false, className = "" }: { article: HomepageArticle; priority?: boolean; className?: string }) {
  if (!article.image) return <div className="flex h-full items-end bg-[linear-gradient(135deg,#0A1931,#00A1DE)] p-4 text-xs font-bold uppercase tracking-[0.16em] text-white/80">Tuganire News</div>
  return <Image src={article.image} alt={article.imageAlt} fill priority={priority} className={`object-cover transition-transform duration-500 group-hover:scale-[1.03] ${className}`} sizes="(max-width: 768px) 100vw, 50vw" />
}

function StoryMeta({ article, locale }: { article: HomepageArticle; locale: Locale }) {
  return <div className="flex flex-wrap items-center gap-2 text-xs text-slate-500 dark:text-slate-400"><span>{formatKigaliDate(article.publishedAt, locale)}</span><span aria-hidden>·</span><span className="inline-flex items-center gap-1"><Clock3 className="size-3" />{article.readTime} min read</span></div>
}

function ShareButtons({ article, onToast }: { article: HomepageArticle; onToast: (message: string) => void }) {
  const url = `https://www.tuganire.site/articles/${article.slug}`
  const encodedUrl = encodeURIComponent(url)
  const encodedTitle = encodeURIComponent(article.title)
  const copyLink = async () => { try { await navigator.clipboard.writeText(url); onToast("Link copied") } catch { onToast("Copy unavailable") } }
  return <div className="flex items-center gap-1" aria-label="Share story"><a href={`https://x.com/intent/post?text=${encodedTitle}&url=${encodedUrl}`} target="_blank" rel="noreferrer" aria-label="Share on X" className="rounded-full p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800"><X className="size-4" /></a><a href={`https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}`} target="_blank" rel="noreferrer" aria-label="Share on Facebook" className="rounded-full p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800"><Facebook className="size-4" /></a><a href={`https://wa.me/?text=${encodedTitle}%20${encodedUrl}`} target="_blank" rel="noreferrer" aria-label="Share on WhatsApp" className="rounded-full p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800"><Send className="size-4" /></a><button type="button" onClick={copyLink} aria-label="Copy story link" className="rounded-full p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800"><Copy className="size-4" /></button></div>
}

function SearchOverlay({ articles, locale, onClose }: { articles: HomepageArticle[]; locale: Locale; onClose: () => void }) {
  const [query, setQuery] = useState("")
  const [recents, setRecents] = useState<string[]>([])
  useEffect(() => { try { setRecents(JSON.parse(localStorage.getItem(SEARCH_RECENTS_KEY) || "[]")) } catch { setRecents([]) } }, [])
  useEffect(() => { const close = (event: KeyboardEvent) => { if (event.key === "Escape") onClose() }; window.addEventListener("keydown", close); return () => window.removeEventListener("keydown", close) }, [onClose])
  const matches = useMemo(() => { const term = query.trim().toLowerCase(); if (!term) return []; return articles.filter((article) => `${article.title} ${article.excerpt} ${article.category}`.toLowerCase().includes(term)).slice(0, 6) }, [articles, query])
  const useRecent = (value: string) => { setQuery(value); const next = [value, ...recents.filter((item) => item !== value)].slice(0, 5); setRecents(next); localStorage.setItem(SEARCH_RECENTS_KEY, JSON.stringify(next)) }
  return <div className="fixed inset-0 z-[70] bg-[#0A1931]/80 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-label="Search Tuganire News"><div className="mx-auto mt-14 max-w-2xl overflow-hidden rounded-2xl bg-white shadow-2xl dark:bg-slate-950"><div className="flex items-center gap-3 border-b border-slate-200 p-4 dark:border-slate-800"><Search className="size-5 text-[#00A1DE]" /><input autoFocus value={query} onChange={(event) => setQuery(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && query.trim()) useRecent(query.trim()) }} placeholder="Search reporting, topics or reporters" className="min-w-0 flex-1 bg-transparent text-base outline-none" /><button type="button" onClick={onClose} aria-label="Close search"><X className="size-5" /></button></div><div className="max-h-[65vh] overflow-y-auto p-4">{!query && recents.length > 0 && <div className="mb-4"><p className="mb-2 text-xs font-bold uppercase tracking-[0.16em] text-slate-500">Recent searches</p><div className="flex flex-wrap gap-2">{recents.map((item) => <button type="button" key={item} onClick={() => useRecent(item)} className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold dark:bg-slate-800">{item}</button>)}</div></div>}{query && matches.length === 0 && <p className="py-8 text-center text-sm text-slate-500">No matching stories yet.</p>}{matches.map((article) => <Link key={article.id} href={`/${locale}/articles/${article.slug}`} onClick={onClose} className="flex gap-3 border-b border-slate-200 py-3 last:border-0 dark:border-slate-800"><div className="relative size-16 shrink-0 overflow-hidden rounded-lg bg-slate-100 dark:bg-slate-900"><StoryImage article={article} /></div><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-[#00A1DE]">{article.category}</p><p className="text-sm font-bold leading-snug">{article.title}</p></div></Link>)}</div></div></div>
}

function NewsletterCard({ locale }: { locale: Locale }) {
  const [email, setEmail] = useState("")
  const [status, setStatus] = useState<"idle" | "loading" | "success" | "error">("idle")
  const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) { setStatus("error"); return }; setStatus("loading"); try { const response = await fetch("/api/subscribe", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email, locale }) }); if (!response.ok) throw new Error("Subscription endpoint unavailable"); setStatus("success"); setEmail("") } catch { setStatus("error") } }
  return <div className="rounded-xl border border-[#00A1DE]/25 bg-[#e9f8fd] p-5 dark:bg-[#082b3d]"><div className="mb-3 flex items-center gap-2"><div className="grid size-9 place-items-center rounded-full bg-[#00A1DE] text-white"><Send className="size-4" /></div><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#0087b8]">Newsletter</p><h3 className="font-black text-slate-950 dark:text-white">Stay informed. Stay Tuganire.</h3></div></div><p className="mb-4 text-sm leading-6 text-slate-600 dark:text-slate-300">A concise briefing from Rwanda and East Africa, delivered when it matters.</p>{status === "success" ? <p className="flex items-center gap-2 text-sm font-bold text-[#008a45]"><Check className="size-4" />You are on the list.</p> : <form onSubmit={submit} className="space-y-2"><label htmlFor="homepage-newsletter-email" className="sr-only">Email address</label><div className="flex gap-2"><input id="homepage-newsletter-email" type="email" required value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" className="min-w-0 flex-1 rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm outline-none ring-[#00A1DE] focus:ring-2 dark:border-slate-700 dark:bg-slate-950" /><button type="submit" disabled={status === "loading"} className="rounded-lg bg-[#0A1931] px-3 py-2 text-sm font-bold text-white disabled:opacity-60 dark:bg-white dark:text-[#0A1931]">{status === "loading" ? "..." : "Join"}</button></div>{status === "error" && <p className="text-xs font-semibold text-red-600">Enter a valid email or try again shortly.</p>}</form>}<a href="https://wa.me/250780000000" className="mt-3 inline-flex items-center gap-1 text-xs font-bold text-[#0087b8] hover:underline">Join our WhatsApp channel <ArrowRight className="size-3" /></a></div>
}

export default function HomepageEditorial({ articles, hero, mostRead, locale }: HomepageEditorialProps) {
  const [searchOpen, setSearchOpen] = useState(false)
  const [mostReadMode, setMostReadMode] = useState<"read" | "shared">("read")
  const [visibleCount, setVisibleCount] = useState(6)
  const [bookmarks, setBookmarks] = useState<string[]>([])
  const [toast, setToast] = useState<string | null>(null)
  const secondary = articles.filter((article) => article.slug !== hero?.slug).slice(0, 4)
  const displayedMostRead = mostReadMode === "read" ? mostRead : [...mostRead].sort((a, b) => (b.views || 0) - (a.views || 0))
  useEffect(() => { try { setBookmarks(JSON.parse(localStorage.getItem(BOOKMARKS_KEY) || "[]")) } catch { setBookmarks([]) } }, [])
  useEffect(() => { if (!toast) return; const timer = window.setTimeout(() => setToast(null), 2200); return () => window.clearTimeout(timer) }, [toast])
  const toggleBookmark = (article: HomepageArticle) => { const next = bookmarks.includes(article.slug) ? bookmarks.filter((slug) => slug !== article.slug) : [...bookmarks, article.slug]; setBookmarks(next); localStorage.setItem(BOOKMARKS_KEY, JSON.stringify(next)); setToast(next.includes(article.slug) ? "Saved to your reading list" : "Removed from your reading list") }
  if (!hero && !secondary.length) return null

  const sectionText = {
    brand: t("brand", locale),
    latestBreakingStories: t("latestBreakingStories", locale),
    verifiedReporting: t("verifiedReporting", locale),
    newsroomFeed: t("newsroomFeed", locale),
    latestNews: t("latestNewsSection", locale),
    viewAll: t("viewAll", locale),
    mostRead: t("mostRead", locale),
    shared: t("shared", locale),
  }

  return <>
    <div className="mx-auto flex max-w-7xl justify-end px-4 pt-4 sm:px-6 lg:px-8"><button type="button" onClick={() => setSearchOpen(true)} className="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-3 py-2 text-xs font-bold text-slate-700 shadow-sm hover:border-[#00A1DE] dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200" aria-label="Search stories"><Search className="size-4 text-[#00A1DE]" />Search newsroom</button><Link href="/dashboard/public/saved" className="ml-2 inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-3 py-2 text-xs font-bold text-slate-700 shadow-sm hover:border-[#00A1DE] dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200"><Bookmark className="size-4 text-[#00A1DE]" />Saved</Link></div>
    <section className="mx-auto max-w-7xl px-4 pb-8 pt-5 sm:px-6 lg:px-8 lg:pt-8"><div className="mb-5 flex items-end justify-between border-b-2 border-slate-950 pb-3 dark:border-white"><div><p className="mb-1 text-[10px] font-bold uppercase tracking-[0.24em] text-[#00A1DE]">Tuganire News</p><h1 className="text-2xl font-black tracking-tight text-slate-950 dark:text-white sm:text-3xl">Latest Breaking News &amp; Stories</h1></div><span className="hidden text-xs font-semibold text-slate-500 sm:block">Verified reporting from Kigali and beyond</span></div><div className="grid gap-6 lg:grid-cols-[minmax(0,1fr)_280px]"><div className="grid gap-5 md:grid-cols-[minmax(0,1.3fr)_minmax(250px,0.7fr)]">{hero && <article className="group relative min-h-[390px] overflow-hidden rounded-xl bg-[#0A1931] text-white shadow-xl md:min-h-[475px]"><Link href={`/${locale}/articles/${hero.slug}`} className="absolute inset-0"><StoryImage article={hero} priority className="opacity-85" /></Link><div className="pointer-events-none absolute inset-0 bg-gradient-to-t from-[#061326] via-[#061326]/40 to-transparent" /><div className="absolute inset-x-0 bottom-0 p-5 sm:p-7"><div className="mb-3 flex flex-wrap items-center gap-2 text-xs font-bold uppercase tracking-[0.18em] text-[#FAD201]">{hero.breaking && <span className="inline-flex items-center gap-1.5 rounded-full bg-red-600 px-2 py-1 text-[10px] text-white"><span className="size-1.5 animate-pulse rounded-full bg-white" />Breaking</span>}<span>{hero.category}</span></div><h2 className="max-w-2xl text-3xl font-black leading-[1.02] tracking-tight text-white sm:text-4xl lg:text-5xl"><Link href={`/${locale}/articles/${hero.slug}`}>{hero.title}</Link></h2><p className="mt-3 line-clamp-2 max-w-xl text-sm leading-6 text-slate-200">{cleanArticlePreview(hero.excerpt, 150)}</p><div className="mt-4"><StoryMeta article={hero} locale={locale} /></div></div></article>}<div className="grid gap-4 sm:grid-cols-2 md:grid-cols-1">{secondary.slice(0, 3).map((article) => <article key={article.id} className="group grid grid-cols-[112px_minmax(0,1fr)] gap-3 border-b border-slate-200 pb-4 dark:border-slate-800"><Link href={`/${locale}/articles/${article.slug}`} className="relative aspect-[4/3] overflow-hidden rounded-lg bg-slate-100 dark:bg-slate-900"><StoryImage article={article} /></Link><div><p className="mb-1 text-[10px] font-bold uppercase tracking-[0.14em] text-[#00A1DE]">{article.category}</p><h3 className="line-clamp-3 text-sm font-bold leading-snug text-slate-950 group-hover:text-[#0087b8] dark:text-white"><Link href={`/${locale}/articles/${article.slug}`}>{article.title}</Link></h3><div className="mt-2"><StoryMeta article={article} locale={locale} /></div></div></article>)}</div></div><aside className="border-t-4 border-[#FAD201] bg-[#0A1931] p-4 text-white lg:sticky lg:top-24"><div className="mb-4 flex items-center justify-between"><div className="flex gap-1 rounded-lg bg-white/10 p-1 text-[11px] font-bold"><button type="button" onClick={() => setMostReadMode("read")} className={`rounded-md px-2 py-1 ${mostReadMode === "read" ? "bg-white text-[#0A1931]" : "text-white/70"}`}>Most Read</button><button type="button" onClick={() => setMostReadMode("shared")} className={`rounded-md px-2 py-1 ${mostReadMode === "shared" ? "bg-white text-[#0A1931]" : "text-white/70"}`}>Shared</button></div><Eye className="size-4 text-[#FAD201]" /></div><ol className="divide-y divide-white/15">{displayedMostRead.slice(0, 5).map((article, index) => <li key={article.id} className="group flex gap-3 py-4 first:pt-0"><span className="text-3xl font-black leading-none text-[#00A1DE]">{String(index + 1).padStart(2, "0")}</span><Link href={`/${locale}/articles/${article.slug}`} className="text-sm font-bold leading-snug text-white group-hover:text-[#FAD201]">{article.title}</Link></li>)}</ol></aside></div><div className="mt-7 hidden min-h-[90px] items-center justify-center border border-dashed border-slate-300 bg-white text-[10px] font-bold uppercase tracking-[0.2em] text-slate-400 sm:flex dark:border-slate-700 dark:bg-slate-900/40">Advertisement · 728 × 90</div></section>
    <section className="border-y border-slate-200 bg-white py-10 dark:border-slate-800 dark:bg-slate-950"><div className="mx-auto grid max-w-7xl gap-10 px-4 sm:px-6 lg:grid-cols-[minmax(0,1fr)_300px] lg:px-8"><div><div className="mb-6 flex items-end justify-between border-b border-slate-200 pb-3 dark:border-slate-800"><div><p className="mb-1 text-[10px] font-bold uppercase tracking-[0.2em] text-[#00A1DE]">The newsroom feed</p><h2 className="text-2xl font-black text-slate-950 dark:text-white">Latest News</h2></div><Link href={`/${locale}/articles`} className="inline-flex items-center gap-1 text-sm font-bold text-[#0087b8]">View all <ArrowRight className="size-4" /></Link></div><div className="divide-y divide-slate-200 dark:divide-slate-800">{articles.slice(0, visibleCount).map((article) => <article key={article.id} className="group grid gap-4 py-5 first:pt-0 sm:grid-cols-[200px_minmax(0,1fr)]"><Link href={`/${locale}/articles/${article.slug}`} className="relative aspect-[16/10] overflow-hidden rounded-lg bg-slate-100 dark:bg-slate-900"><StoryImage article={article} /></Link><div><div className="mb-1 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.16em] text-[#00A1DE]">{article.breaking && <span className="inline-flex items-center gap-1 text-red-600"><span className="size-1.5 animate-pulse rounded-full bg-red-600" />Live</span>}<span>{article.category}</span>{article.sponsored && <span className="text-slate-500">Sponsored</span>}</div><h3 className="text-xl font-bold leading-tight text-slate-950 group-hover:text-[#0087b8] dark:text-white"><Link href={`/${locale}/articles/${article.slug}`}>{article.title}</Link></h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-600 dark:text-slate-300">{cleanArticlePreview(article.excerpt, 180)}</p><div className="mt-3 flex flex-wrap items-center justify-between gap-2"><StoryMeta article={article} locale={locale} /><div className="flex items-center gap-1"><button type="button" onClick={() => toggleBookmark(article)} aria-label={bookmarks.includes(article.slug) ? "Remove bookmark" : "Bookmark story"} className="rounded-full p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800">{bookmarks.includes(article.slug) ? <BookmarkCheck className="size-4 text-[#00A1DE]" /> : <Bookmark className="size-4" />}</button><ShareButtons article={article} onToast={setToast} /></div></div></div></article>)}</div>{visibleCount < articles.length ? <button type="button" onClick={() => setVisibleCount((count) => Math.min(count + 4, articles.length))} className="mt-5 inline-flex items-center gap-2 rounded-full border border-slate-300 px-5 py-2.5 text-sm font-bold text-slate-900 hover:border-[#00A1DE] dark:border-slate-700 dark:text-white"><span className="size-2 animate-pulse rounded-full bg-[#00A1DE]" />Load more stories</button> : <div className="mt-5 grid gap-3 sm:grid-cols-3" aria-label="More stories loading"><div className="h-3 animate-pulse rounded bg-slate-200 dark:bg-slate-800" /><div className="h-3 animate-pulse rounded bg-slate-200 dark:bg-slate-800" /><div className="h-3 animate-pulse rounded bg-slate-200 dark:bg-slate-800" /></div>}</div><aside className="space-y-5 lg:sticky lg:top-24 lg:self-start"><NewsletterCard locale={locale} /><div className="flex min-h-[250px] items-center justify-center border border-dashed border-slate-300 bg-slate-50 text-[10px] font-bold uppercase tracking-[0.2em] text-slate-400 dark:border-slate-700 dark:bg-slate-900">Advertisement · 300 × 250</div><div className="rounded-xl border border-slate-200 p-5 dark:border-slate-800"><h2 className="mb-3 font-black text-slate-950 dark:text-white">Explore Topics</h2><div className="flex flex-wrap gap-2">{["Rwanda", "Kigali", "East Africa", "Public interest", "Startups", "Elections"].map((topic) => <Link key={topic} href={`/${locale}/search?q=${encodeURIComponent(topic)}`} className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold text-slate-700 hover:bg-[#00A1DE] hover:text-white dark:bg-slate-800 dark:text-slate-200">{topic}</Link>)}</div></div></aside></div></section>
    <section className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="grid gap-6 rounded-2xl bg-[#0A1931] p-6 text-white shadow-2xl sm:p-10 lg:grid-cols-[1fr_0.8fr] lg:items-center"><div><p className="mb-3 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] text-[#FAD201]"><Sparkles className="size-4" /> Tuganire Investigates</p><h2 className="max-w-2xl text-3xl font-black leading-tight sm:text-4xl">Reporting that follows the evidence, wherever it leads.</h2><p className="mt-4 max-w-xl text-sm leading-7 text-slate-300">Our investigations put public interest first, with transparent sourcing and editorial accountability across Rwanda and East Africa.</p><div className="mt-6 flex flex-wrap gap-3 text-xs font-bold"><span className="inline-flex items-center gap-1.5 rounded-full bg-white/10 px-3 py-2"><ShieldCheck className="size-4 text-[#00A1DE]" /> Verified</span><span className="inline-flex items-center gap-1.5 rounded-full bg-white/10 px-3 py-2"><ShieldCheck className="size-4 text-[#FAD201]" /> Fact-checked</span><span className="inline-flex items-center gap-1.5 rounded-full bg-white/10 px-3 py-2"><ShieldCheck className="size-4 text-[#00A651]" /> Transparent</span></div></div><div className="relative min-h-[220px] overflow-hidden rounded-xl border border-white/15 bg-[#113052]"><div className="absolute inset-0 bg-[radial-gradient(circle_at_70%_20%,rgba(0,161,222,0.4),transparent_45%)]" /><div className="absolute bottom-5 left-5 max-w-xs"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#00A1DE]">In focus</p><p className="mt-2 text-xl font-bold">Following the decisions that shape everyday life.</p></div></div></div></section>
    {searchOpen && <SearchOverlay articles={articles} locale={locale} onClose={() => setSearchOpen(false)} />}{toast && <div role="status" className="fixed bottom-5 left-1/2 z-[80] -translate-x-1/2 rounded-full bg-[#0A1931] px-4 py-2 text-sm font-bold text-white shadow-xl">{toast}</div>}
  </>
}
